language: bash

before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - sleep 3 # give xvfb some time to start
  - chmod +x tma_v2.1-setup.sh

script:
    - ./tma_v2.1-setup.sh
    - tma
